services:
  - type: web
    name: auto-pilot-backend
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node dist/index.js
    rootDirectory: .
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
